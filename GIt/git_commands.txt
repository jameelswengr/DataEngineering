 git config --global user.name "jameel ahmed" 
 git config --global user.email jameel.swengr@gmail.com            
 git config --global core.editor "code --wait"
 git config --global -e
 git config --global core.autocrlf true

 Commiting best practices.
 1. commit often
  As you reach a state you want to record then make a commit. 
    1. commit for  bug fix
    2. commit for typo.

Do we need to stage the changes every time before commit.
git commit -a -m "commit in a single line"



